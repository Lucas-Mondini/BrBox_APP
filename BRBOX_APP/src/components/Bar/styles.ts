import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  bar: {
    height: 15,
    borderRadius: 8,
    overflow: "hidden",
    marginVertical: 35,
    flexDirection: "row",
  }
});

export default styles;